import React from 'react'
import { connect } from 'react-redux'

class GroupPage extends React.Component {
    render () {
        return (
            <div>groups</div>
        )
    }
}

export default connect(null, null)(GroupPage)